# Committed on purpose. These are client-side values that ship inside the
# browser bundle and are public by design — the same way the Firebase web
# config in firebase-applet-config.json is public.
#
# IMPORTANT: restrict the Maps key by HTTP referrer in Google Cloud Console
# (APIs & Services -> Credentials) to https://crm.gostratos.vc/* so it can't
# be reused on someone else's site.

VITE_GOOGLE_MAPS_API_KEY="AIzaSyBFvCenAYgcRKQWGKnzDBDfbwmWePazPQE"
